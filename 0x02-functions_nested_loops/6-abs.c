#include "main.h"
#include <stdio.h>
/**
 * _abs - print the absolute value of an integer
 * Return: void
 * @int: absolute int
 */
int _abs(int)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (0);
	}
}
